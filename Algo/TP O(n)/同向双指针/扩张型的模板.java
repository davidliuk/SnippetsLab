// [i, j]
// 擅长一次性找到最大的右边界
int j = 1;
for (int i = 0; i < nums.length; i++) {
    while (j < nums.length && i,j的搭配不满足条件) {
        j++;
    }
    if (j >= nums.length) {
        break;
    }
    处理i, j这次搭配
}